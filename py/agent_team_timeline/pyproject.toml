[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-team-timeline"
version = "0.1.0"
description = "Build durable, multi-resolution timelines from coordinator and subagent transcripts."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Ryan Newton" }]
keywords = ["agents", "timeline", "transcripts", "visualization"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Operating System :: POSIX",
]
dependencies = []

[project.optional-dependencies]
dev = ["mypy", "pytest"]

[project.urls]
Repository = "https://github.com/rrnewton/agent-utils"
Issues = "https://github.com/rrnewton/agent-utils/issues"

[project.scripts]
agent-team-timeline = "agent_team_timeline.cli:main"

[tool.setuptools]
packages = ["agent_team_timeline"]
package-dir = { agent_team_timeline = "." }
include-package-data = false

[tool.setuptools.package-data]
agent_team_timeline = [
  "py.typed",
  "README.md",
  "USER_GUIDE.md",
  "static/*.html",
  "static/*.js",
  "static/*.css",
  "static/vendor/*.js",
  "static/vendor/*.txt",
  "static/vendor/*.md",
]
