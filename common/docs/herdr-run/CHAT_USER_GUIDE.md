# Agent-control documentation

Run `agentctl chat userguide` for the installed operator reference. Agent control and
Chat are provided by the independent `agentctl` package. The CLI documentation
is maintained in that package; this path remains a documentation redirect.
