[profile.default]
fail-fast = false
slow-timeout = { period = "1s", terminate-after = 1, grace-period = "1s" }

[profile.outer-backstop]
inherits = "default"
slow-timeout = { period = "60s", terminate-after = 1, grace-period = "1s" }
